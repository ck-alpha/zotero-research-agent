/** Minimal evidence contract; no retrieval or generated explanation here. */
export interface CandidateEvidence {
  /** Opaque references to evidence owned by the research data layer. */
  evidenceRefs: string[];
}
